// testing use cases: 
// 1) User has requested an available date
// 2) User has requested an unavailable date
// 3) Base pass user has requested a blackout date
// 4) Full pass user has requested a blackout date
// 5) User has requested a date in the past


var assert = require('assert');
describe('Array', function() {
  describe('#indexOf()', function() {
    it('should return -1 when the value is not present', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});

console.log("hello there")