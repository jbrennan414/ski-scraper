async function removeDate(date, resort, chatID) {
    console.log("here is the resort bbbbb", resort)
    console.log("here is the date bbbbb", date)
    console.log('here is the chat id', chatID)
}

module.exports = removeDate;